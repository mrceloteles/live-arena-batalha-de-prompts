# Resultado da regressão do motor — 2026-09-04

## Veredito

O motor ainda não pode ser declarado 100% idêntico ao backend privado. A execução encontrou e corrigiu uma divergência observável no início do ciclo, demonstrou equivalência do fluxo clássico dentro do alfabeto testado e confirmou 100% dos três únicos scores independentes recuperados. Três exemplos não são suficientes para generalizar a fórmula semântica do juiz.

Uma segunda execução remota isolada encontrou duas divergências em relação ao pacote histórico: o site vivo aplica bônus de velocidade aos pontos e seu fallback atual não reproduziu a antiga fórmula no caso observado. O timeout vazio, por outro lado, foi capturado integralmente e agora é reproduzido pelo motor.

## Contraexemplo vivo de pontuação e timeout

- sala isolada: `fidelity-audit-20260904`, sem uso da sala `main`;
- match 4981: 20%, envio em 47/60 s, 2.217 pontos e `fallback_scored`;
- ajuste exato observado: `round(percent * 100 + ((duration - elapsed) / duration) * 1000)`;
- match 4982: prompt vazio enviado no deadline, `timeout_empty`, 0%, 0 pontos e `fallback_used: false`;
- o fallback histórico calcula 11,7221% para o primeiro par, mas o vivo devolveu 20%; por isso a equivalência do fallback atual foi rebaixada para `INFERRED`.

Artefato bruto normalizado: `evidence/live-isolated-cycle-2026-09-04.json`.

## Achado corrigido

O endpoint real `room_status` retornou `phase: idle` antes do primeiro cadastro. Nosso ciclo começava em `registration`.

Correção aplicada:

- um novo ciclo nasce em `idle`;
- o primeiro cadastro promove o ciclo para `registration`;
- ao terminar os 30 segundos do ranking final, o novo ciclo volta a `idle`;
- configuração do modo turma continua permitida antes do primeiro cadastro.

## AALpy

- alvo: motor atual em memória, consultado pela mesma API de domínio do servidor;
- alfabeto: `status`, `register_all`, `accept_all`, `submit_all`, `advance_window`;
- versão: 1.6.2;
- seed: 20260904;
- orçamento do oráculo: 4.000 passos;
- probabilidade de reset: 0,12;
- hipótese aprendida: 13 estados;
- rodadas de aprendizagem: 1;
- consultas de pertinência: 325 durante aprendizagem e 470 durante conformidade;
- passos executados: 2.600 durante aprendizagem e 4.000 durante conformidade;
- artefatos: `aalpy/model.dot` e `aalpy/run.json`.

O primeiro ensaio produziu 38 estados e revelou corrupção interna do adaptador após uma macro rejeitada. Foi criado um teste de regressão, observado em vermelho, corrigido e reexecutado. O modelo final convergiu para 13 estados sem esse ruído.

## ai-system-testing

O dataset dourado contém somente os três scores armazenados no pacote fornecido pelo usuário. Cada score foi repetido dez vezes, com exigência de 100% de aprovação e conversão literal de percentual para pontos.

Resultado: 30/30 comparações do candidato aprovadas. Esse resultado prova equivalência apenas nesses três fixtures.

O patamar recomendado de 50 casos independentes por comportamento de IA ainda não foi alcançado: faltam 47 observações legítimas do juiz de referência.

## Promptfoo

Matriz executada com:

- provedor `source-package-reference`;
- provedor `current-source-fallback`;
- três casos;
- dez repetições por caso e por provedor;
- assertion determinística `equals`;
- cache desativado.

Resultado verificado: 60/60 avaliações aprovadas, zero falhas. O resultado completo está em `promptfoo/output.json`.

## O que falta para uma afirmação de 100%

1. Coletar os 45 pares ainda restantes: duas das 47 observações adicionais foram concluídas, mas nenhuma passou pelo Gemini porque o site retornou `fallback_scored`/`timeout_empty`.
2. Observar diretamente os ramos de empate integral do ranking original.
3. Observar o timeout com prompt não vazio; o timeout vazio já foi capturado e reproduzido.
4. Capturar o ciclo administrativo original após autenticação autorizada.
5. Inferir o sistema real com reset reproduzível. Sem reset autorizado, AALpy não pode aprender exaustivamente a máquina remota.

## Reprodução

```bash
python3 .agents/skills/aalpy-state-inference/scripts/infer_mealy.py \
  --adapter research/fidelity-lab/aalpy/local-classic-adapter.py \
  --output-dir research/fidelity-lab/aalpy \
  --steps 4000 --reset-probability 0.12 --seed 20260904

npx promptfoo eval \
  -c research/fidelity-lab/promptfoo/promptfooconfig.yaml \
  -o research/fidelity-lab/promptfoo/output.json \
  --repeat 10 --no-cache --no-share

node --test test/research-fidelity-toolchain.test.mjs
```
